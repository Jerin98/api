const { List } = require('./list.model');
const { Task } = require('./task.model');
const { User } = require('./user.model');
const { Timetable } = require('./timetable.model');
const { Ttask } = require('./ttask.model');

module.exports = {
    List,
    Task,
    User,
    Timetable,
    Ttask
    
}